[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.coverage.run]
omit = [
    "app/main.py",
    "app/middleware/*",
    "app/db/session.py",
    "app/workers/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__",
    "pass",
]
